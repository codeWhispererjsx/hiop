# Hospitality operations

Epic 1D adds configurable hospitality asset categories and types, property-scoped
technology services, device-to-service relationships, and bounded property
operations summaries. The `/operations` page consumes the operations summary API
and keeps monitoring separate from direct PMS, POS, CCTV, door-lock, PBX, and
building-management integrations, which remain out of scope.
