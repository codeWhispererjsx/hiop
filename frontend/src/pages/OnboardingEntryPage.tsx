import {Link} from "react-router-dom";
import DashboardLayout from "../layouts/DashboardLayout";
import {Icon} from "../components/Icon";
export default function OnboardingEntryPage(){let context:{organization?:{name:string};property?:{name:string}}={};try{context=JSON.parse(window.sessionStorage.getItem("hiop.onboarding")||"{}")}catch{/* use fallback */}return <DashboardLayout><section className="first-run"><p className="page-kicker">Workspace ready</p><h1>Welcome to HIOP</h1><p>Your isolated organization workspace is ready. Start with discovery when your property network is prepared.</p><div className="first-run-context"><div><span>Organization</span><strong>{context.organization?.name||"Current organization"}</strong></div><div><span>Property</span><strong>{context.property?.name||"Current property"}</strong></div></div><h2>HIOP setup</h2><ol><li className="done"><Icon name="check"/>Organization created</li><li className="done"><Icon name="check"/>Property created</li><li><span/>Configure discovery</li><li><span/>Connect local agent</li><li><span/>Run first scan</li><li><span/>Approve first devices</li><li><span/>Configure monitoring</li></ol><div className="first-run-actions"><Link className="primary-action" to="/discovery-intelligence">Configure discovery</Link><Link className="secondary-action" to="/dashboard">Open dashboard</Link></div></section></DashboardLayout>}
