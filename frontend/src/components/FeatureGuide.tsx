const guides = {
  snmp: {title:"SNMP, step by step", body:<><p>Imagine a printer has a little information desk. HIOP asks, “Who are you? Are you working? How much toner is left?” SNMP is the language they use to answer.</p><ol><li><strong>Choose a supported device.</strong> A printer, switch, router, or access point may support SNMP. Check its administration page or manual.</li><li><strong>Turn on read-only SNMP on that device.</strong> Prefer SNMPv3 with authentication and encryption. Create a monitoring username and secrets. Older devices may use SNMPv2c with a community string, which acts like a shared password.</li><li><strong>Add the same credential in HIOP.</strong> The version, username, authentication, encryption, and secrets must match the device.</li><li><strong>Add a target.</strong> Choose the correct property, enter the device’s private IP address, and attach the credential.</li><li><strong>Test the target.</strong> HIOP’s monitoring server must be able to reach the device. SNMP usually uses UDP port 161. Ask IT to allow that traffic from the monitoring server; do not turn off the firewall.</li><li><strong>Poll and review.</strong> A successful poll can return identity, uptime, interface statistics, or other values the device supports. Different devices return different information.</li></ol><p><strong>For hosted HIOP:</strong> SNMP collection currently runs on the backend. The local agent does not yet have an SNMP adapter. A reachable on-site backend or a private network connection is required. An online agent alone does not make SNMP work.</p><p><strong>If the test fails:</strong> check the IP, SNMP version, credential, allowed source address, and network route. A timeout means no reply arrived; it does not prove the device is switched off.</p></>},
  ad: {title:"How Active Directory works in HIOP",body:<><p>Active Directory is your organisation’s address book for staff and computers. This HIOP integration reads computer records to help identify discovered devices; it does not automatically enable staff sign-in.</p><ol><li>Ask your IT administrator for the domain-controller address and a service account that can read computer records.</li><li>Enter your domain, secure LDAP server and port. LDAPS commonly uses 636; StartTLS commonly uses 389.</li><li>Enter the Base DN: the starting folder to search, such as DC=example,DC=com. A computer search base can narrow this to a specific folder.</li><li>Save the connection and use Test connection. The backend must reach the directory server and trust its certificate.</li><li>Enable the connection, run Active Directory enrichment from Discover, and review the identity suggestions.</li></ol><p>HIOP does not modify directory objects. The current local agent has no Active Directory adapter, so hosted collection requires a private network route from the backend. A saved connection is not evidence of a successful test.</p></>},
  agent: {title:"How to connect a local agent",body:<><p>The agent is a helper that stays inside your property’s network. You use HIOP in a browser; the helper checks local devices and reports back.</p><ol><li>Choose a Windows computer that stays on and can reach the devices you want to monitor.</li><li>Install the HIOP agent package supplied by your platform operator.</li><li>Select the property and generate a one-time enrollment token. Copy it into the installer or enrollment command before it expires.</li><li>Configure the agent with the HIOP backend address supplied by your operator. Its connection to HIOP uses outbound HTTPS.</li><li>Wait for Online status, select the same property in Discover, and start a small scan.</li></ol><p>Scanning uses ping and a small set of connection checks. A device can be online but block those checks. “No response” is not the same as “powered off.” SNMP and Active Directory are not yet supported by this agent version.</p><p>Terminate scan requests that the agent stop its scan process. HIOP shows Stopping until it receives acknowledgement. If the agent loses its connection, acknowledgement cannot be immediate.</p></>}
};
export function FeatureGuide({kind}:{kind:keyof typeof guides}) { const guide=guides[kind];return <details className="feature-guide"><summary>{guide.title}</summary><div>{guide.body}</div></details>; }
