import { useState, type FormEvent } from "react";
import type { User, UserInput } from "../lib/types";

export default function UserForm({user,roles,busy,onSubmit}:{user?:User;roles:string[];busy:boolean;onSubmit:(value:UserInput)=>Promise<void>}){
 const [form,setForm]=useState<UserInput>(()=>user?{username:user.username,email:user.email,role:user.role,is_active:user.is_active}:{username:"",email:"",password:"",role:"technician",is_active:true}); const [confirm,setConfirm]=useState(""); const [errors,setErrors]=useState<Record<string,string>>({});
 const submit=async(e:FormEvent)=>{e.preventDefault();const next:Record<string,string>={};if(form.username.trim().length<3)next.username="Use at least 3 characters.";if(!/^\S+@\S+\.\S+$/.test(form.email))next.email="Enter a valid email.";if(!user&&(form.password??"").length<10)next.password="Use at least 10 characters.";if(!user&&form.password!==confirm)next.confirm="Passwords do not match.";setErrors(next);if(Object.keys(next).length)return;await onSubmit(form)};
 return <form className="enterprise-form" onSubmit={submit} noValidate><div className="form-grid"><label>Username<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/>{errors.username&&<small className="field-error">{errors.username}</small>}</label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/>{errors.email&&<small className="field-error">{errors.email}</small>}</label>{!user&&<><label>Temporary password<input type="password" autoComplete="new-password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{errors.password&&<small className="field-error">{errors.password}</small>}</label><label>Confirm password<input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/>{errors.confirm&&<small className="field-error">{errors.confirm}</small>}</label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{roles.map(r=><option key={r} value={r}>{r[0].toUpperCase()+r.slice(1)}</option>)}</select></label><label className="checkbox-label"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Account active</label></>}</div><button className="primary-action" disabled={busy}>{busy?'Saving…':user?'Save changes':'Create user'}</button></form>
}
