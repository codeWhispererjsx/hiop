HIOP Desktop 0.1.2

Fixes desktop interface loading on Windows. This release includes the complete local HIOP frontend, backend, PostgreSQL runtime, and Python runtime so HIOP launches without the blank-window issue.
